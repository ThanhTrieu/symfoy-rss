{% extends 'base.html.twig' %}

{% block content %}
    <div class="search menu-search2">
        <div class="main">
            <h1 class="title-18 full-width">{{ cate_name }}</h1>
            {#{% if type == 0 %}
                <ul class="menu-search">
                    {% if array_child is defined %}
                        {% for key, item in array_child %}
                            <li><a href="{{ item.url }}">{{ item.name }}</a></li>
                        {% endfor %}
                    {% endif %}
                </ul>
            {% endif %}#}
        </div>
    </div>
    <div id="wrapper">
        <section class="list-new2">
            <div class="main">
                <ul class="list-news-2" id="pnlPostList">
                    {% include 'category/contents.html.twig' with {'news_list': news_list} only %}
                </ul>
                {% if loadMoreToken is not null %}
                    <div class="text-center">
                        <button id="btnLastestMore" class="btn btn-all" data-token="{{ loadMoreToken }}" data-url="{{ url_ajax }}">Load more</button>
                        <img id="imgMoreLoading" class="hidden" src="/images/loading.gif" title="Please wait while loading data" />
                    </div>
                {% endif %}
            </div>
        </section>
    </div>
{% endblock %}