{% extends 'base.html.twig' %}

{% block content %}
    <section class="list-new2 pd-top-29">
        <div class="container">
            <div class="title-menu">
                <h2 class="title-30-22">VIDEOS</h2>
            </div>
            {% if info != null %}
                <div style="width: 100%;">
                    <div id="mainPlayVideo">
                    </div>
                    <div style="width:359px;float: left;margin-left: 30px; margin-top: -18px;">
                        <h1><a rel="nofollow" href="javascript:void(0);" class="name-pro" id="firstTitle" style="color: #191919;">{{ info['title'] }}</a></h1>
                        <div class="short-info" id="firstSapo">{{ info['description'] | raw }}</div>
                    </div>
                </div>
            {% endif %}
            <div class="clear"></div>
            <ul class="list-news-2 display-ib videos">
                <div class="clear"></div>
                {% if list != null %}
                    {% for key, item in list %}
                        <li id="item-{{ item.video_id }}"
                            class="{{ (key+1)%3==1? 'clear':'' }} {{ (key+1)%3==0? 'last':'' }}">
                            <a class="img-sp iframetrack" rel="{{ item['url'] }}" title="{{ item['title'] | raw }}">
                                <img src="{{ item['avatar'] }}" alt="{{ item['title'] | raw }}">
                                <div class="icon-play"></div>
                            </a>
                            <div class="info-car">
                                <h3 class="list-title"><a href="{{ item['link'] }}" class="name-pro">{{ item['title'] | raw }}</a></h3>
                                <span class="date-creat"> <i class="icon-clock"></i>{{ item['created_date'] }}</span>
                                <p style="display: none;" class="sapoVideos">{{ item['description'] | raw }}</p>
                            </div>
                        </li>
                    {% endfor %}
                {% endif %}
            </ul>
        </div>
        {% if info != null  %}
            <input type="hidden" value="{{ info['url'] }}" id="hddUrlVideos"/>
            <input type="hidden" value="{{ info['avatar'] }}" id="avatarUrlVideos"/>
        {% endif %}
    </section>
{% endblock %}